# SDD ledger — plan: docs/superpowers/plans/2026-09-21-backend-replacement.md

Branch: backend/fastapi-replacement Worktree: /tmp/pkdb-backend-replacement Base: 75e29fd4 Execution: native, authorized by user.

## Pre-flight interfaces
- F1 -> F3/F4/A2: contracts and baseline define source/response semantics; consistent.
- F3 -> F4/I4: SourceBundle and CanonicalStudy; consistent.
- F4 -> I2/I4: versioned Vocabulary/PreparedStudy; consistent.
- I1/I2/I3 -> I4: sessions, Principal, staged files; consistent.
- I4 -> I5/A4/A5: shared ingestion service; consistent.
- A2 -> A3/A5: typed query service; consistent.
- I5 -> A6: optional Settings app factory; corrected during planning.

## Rulings
- Ruling: Worktree placed in /tmp rather than changing the original checkout's ignore rules — preserves ongoing unrelated changes — cost if wrong: workspace relocation.
- Ruling: Run non-executable skill helper scripts through bash — installed files lack executable permissions — no product impact.
- Ruling: Use the master ledger with F1–A6 task IDs; skill task extraction assumes numbered headings absent from the approved plan — read linked task text directly — cost if wrong: bookkeeping only.

## Tasks
- F1: in progress.
- F2/F3/F4/I1/I2/I3/I4/I5/A1/A2/A3/A4/A5/A6: pending.

## Resumed 2026-09-22
- Automatic approval usage limit interrupted execution; user upgraded and resumed.
- Legacy baseline: first run 28 passed/80 setup errors because API_BASE missing during migration generation. Corrected test-only environment; rerun 108 passed in 31.46 s.
- Comparator/manifest/route tooling: 17 tests passed, legacy runtime emits expected deprecation warnings.
- Ruling: Legacy integer SIDs normalize to strings; initial inventory classification was too strict — matches DRF CharField — cost if wrong: identity collisions caught by manifest/constraints.
- Corpus: 1,579 folders, 30 apixaban; all source files read-only. Detailed hashes remain scratch; committed manifest stores per-study combined digest to avoid attachment-name exposure.
- Ruling: F2 packaging proceeds while F1's realistic upload baseline awaits reference-data bootstrap — independent runtime compatibility work remains useful — cost if wrong: packaging may need adjustment after scientific parity.
- F1: route/identity inventory and existing-suite baseline done; actual populated-study upload/read performance and golden payload capture remain pending.
- Ruling: Keep package version 0.10.0 instead of plan example 0.1.0 — preserve repository version — no compatibility cost.
- F2 interpreter pitfall: uv run without --python follows .python-version and recreated the named 313 environment with 3.14. Corrected all CI commands to pass --python explicitly; recreated actual 3.13 environment and reran tests/type checks.
- F2 complete: 3 runtime tests on each of CPython 3.13.1/3.14.6; Ruff/ty clean; sdist/wheel build and clean-wheel scientific imports succeeded. Full scientific/API/container gates belong to later tasks.
- F3/F4 checkpoint: canonical models, read-only workbook/TSV parsing, structured errors, scientific graph/unit validation, normalization and PK calculations implemented in part. 52 tests pass on each actual Python 3.13/3.14; Ruff and ty pass. New individual/group-statistics and duplicate-header regressions were observed failing then passing.
- Apixaban parsing: 28/30 passed; Frost2013a references missing Frost2013a_Tab3.png; Wang2016 Tab3 maps absent label column. Source data left unchanged; these are outstanding gates, not exclusions.
- F3/F4 still pending full scatter expansion, offline vocabulary/bootstrap, legacy golden scientific comparison and full corpus disposition.
- Ruling: Start independent relational schema work while F1/F3/F4 parity gates remain open — canonical entities provide sufficient schema contracts; no cutover is allowed until parity gates pass — cost if wrong: amend unpublished initial schema.
- I1 relational checkpoint: 26 tables with typed measurements, composite study-scoped FKs, ordered points/authors, shared users/vocabulary, attachment metadata. Eight real PostgreSQL constraint tests pass. Full suite: 60/60 on both CPython 3.13 and 3.14. Alembic upgrade/check/downgrade/upgrade passed on isolated PostgreSQL 18; no metadata drift. CI now provisions PostgreSQL only. F1 field-completeness review remains tied to legacy golden gates.
- I2 checkpoint: immutable principals; 46-case authorization suite; tokens stored as SHA-256 digests with expiry/revocation/purpose/active-user checks; offline bootstrap validates duplicate IDs and parent graphs before writing, creates disabled identities, and hashes effective vocabulary under advisory lock. 118 tests pass on each Python, ty clean. Full production vocabulary snapshot and ownership-transfer race tests remain pending.
- I3 file-store checkpoint: fsynced immutable staging with counted byte limits, owner-scoped draft reads, published licence checks, integrity verification, leased expiry cleanup and 24-hour untracked-file grace. Symlink/traversal and published-file protection covered. 132 tests pass on 3.13; 3.14 result checked separately. Final publication/cleanup race tests await I4.
- Ruling: Integration tests each own a random PostgreSQL schema rather than sharing one for the session — independently committed file-store transactions contaminated an unrelated fixture — cost: several seconds of test setup, stronger isolation.
- I3 verification confirmed: 132/132 tests pass on both interpreters.
- I4 checkpoint: typed bulk graph persistence and repeatable-read reconstruction, stable roots, deterministic SID advisory locks, shared vocabulary lock, active-user/ownership checks, immutable-file verification. Exact round-trip, failure after deletion/before commit, concurrent absent SID, old snapshot, mutable-state recheck and 10-vs-1000 SQL-count tests pass. 142 tests passed on both Pythons before HTTP additions. Scatter publication explicitly rejects pending full dataset contract; no data is silently dropped.
- I5 checkpoint: authenticated multipart validate/PUT/read factory endpoints, structured errors, readiness/liveness, counted ASGI body limit, semaphore admission, cancellation release and actual workbook upload. 152 tests pass on each Python; Ruff/ty clean. Starlette emits two dependency deprecations. Full legacy routes, matched benchmarks, all multipart abuse cases and final cutover remain pending.
- Full offline vocabulary export: 2,147 original serialized nodes using copied caches and blocked network access. Eleven optional OLS enrichments were unavailable, recorded in provenance. Bootstrap integration test passes with real apixaban mass/sex choices.
- Ruling: Collapse one repeated vocabulary parent edge — matches legacy many-to-many semantics — cost if wrong: original edge multiplicity (not representable in legacy DB) is not retained.
- Corpus regressions fixed RED→GREEN: comma-separated intervention cells after column mapping; GFR units scaled per 1.73 m²; nonfinite normalization. Conversion compilation cached by unit/rule/molar mass. Apixaban now 28/30 parse and 24/30 scientific validate (52 passed/8 failed across both gates); remaining failures unexcluded.
- Current regular suite: 156 passed on each Python, Ruff/ty clean. Conversion tool test passes.
- F1 populated baseline advanced: offline vocabulary bootstrapped into disposable legacy PostgreSQL; original uploader on a copied Frost2014 directory succeeded in ~6.8s. Initial public read was 404 because legacy indexing is asynchronous. Post-index golden capture remains in progress.
- Legacy baseline requires scratch settings overriding MEDIA_ROOT to writable worktree scratch; initial /media attempt failed and was retried. Sources unchanged.
- F1/F4 numerical golden: Frost2014 has exactly 782 matching scientific records (379 reported + 379 normalized + 12 calculated raw + 12 calculated normalized), 1 group, 70 individuals. Golden fixture compares stable scientific identity, all retained fields and per-measurement tolerances rtol=1e-9/atol=1e-12. Passes on 3.13 and 3.14. Canonical calculated output_type='output' maps legacy empty string explicitly; labels remain null.
- Ruling: Add independent `calculated` provenance flag in addition to origin — legacy outputs can be both calculated and normalized, which the original three-way origin enum cannot represent — cost: one column per scientific table and an explicit migration.
- Derived errors apply to normalized group outputs only, as confirmed in legacy Output.add_error_measures; raw statistics stay reported. Explicit zero errors remain zero per accepted spec. Default sample-mean policy retained when present in vocabulary.
- 159 regular tests pass on each interpreter after these changes, plus the real 782-record golden on each.
- Legacy uploader also rejects all six remaining apixaban failures: Frost2013a/Frost2014a/Frost2015/Frost2018/Kreutz2017/Wang2016. Full response evidence in legacy-apixaban-dispositions.json; no studies edited/excluded.
- A1 checkpoint: account login/registration, verified email reset, one-use verification/reset, session revocation, PostgreSQL throttles, SMTP rollback, owner-scoped email management and protected downloads implemented. Populated user migration regression fixed with server defaults. 178 tests pass on both Python 3.13/3.14; Ruff/ty clean. Admin account/group routes, exact validation error compatibility and remaining media contracts still pending.
- Ruling: verify_email returns the verified address internally rather than None — legacy HTTP response includes email — no external compatibility cost.
- Ruling: duplicate email creation does not return another user's address record — legacy serializer could disclose ID/verification; return a neutral response — cost: duplicate-address response differs intentionally.
- Ruling: unique service test filename avoids pytest import-name collision — no runtime effect.
- A2 started with observed failing same-measurement, privacy/count, pagination, deduplication and input allowlist tests.
- A2 checkpoint: shared parameterized PostgreSQL query service; same-measurement EXISTS scopes; visibility before pagination/count/statistics; strict scalar/list SQL value validation; public output/group/individual/intervention adapters with explicit schemas and batched relationship loads. Recursive CTE subject ancestry preserves overrides and additive disease/abstinence characteristics.
- Real persisted Frost2014 golden passes on both Pythons: 782 scientific outputs, 70 individual responses with inherited characteristics, 1 group, 4 raw/normalized intervention responses, and all nine legacy count fields. Individual characteristic default count=1 restored after observed golden failure; normalized-only timecourse count restored after observed 8-vs-4 failure.
- 190 regular tests pass on each Python; the subsequently added intervention HTTP test passes separately on each (191 total tests now). Ruff/ty pass; final new test formatted. A2 remains open for study/reference/vocabulary/dataset reads, comprehensive filter/search mappings, full-text indexes and benchmarks.
- Ruling: Move database fixture plugin registration to backend-next/conftest.py — explicit corpus tests require isolated schema fixtures too — no product behavior change.
- A2 reference checkpoint: visibility-scoped reference list/detail, ordered authors, real integer author identifiers and migration metadata checks. Frost2014 reference payload/order joins the public scientific golden.
- Ruling: Add a generated integer author identifier alongside the ordered reference/position key — legacy public responses expose integer author IDs — cost: one unique integer column; stable reference SIDs and author order remain the scientific identity.
- F3/F4/I4 dataset checkpoint: legacy dimension labels/shared fields, mapped rows grouped by dataset name, exact normalized x/y pairing, bulk typed dimension persistence, descriptions/comments and canonical readback. Unknown comment authors now fail instead of losing attribution.
- Scientific/importer regressions fixed RED→GREEN: nested characteristic || expansion; [username,text] comments; blank mapped intervention cells; structural rather than formatted-string unit dimensionality matching; label-scoped timecourses across templates; minimum two timecourse points; deterministic binary attachment ordering.
- Ruling: Represent automatically generated normalized timecourse subsets in the existing dataset/subset tables — legacy auto-generates a single AutoGenerate data record; avoids colliding public identifiers from two unrelated tables — cost: normalized timecourse point associations are also retained for public dataset reads.
- Source-declared timecourses remain rejected as in legacy; internal generation uses typed DataRecord/Subset structures. Processing version advanced to 2 for changed timecourse grouping.
- Real caffeine publication passed on Python 3.13: Abernethy1985a (78 scatter pairs), Akinyinka2000 (91), Begas2015 (31), including exact attachments and comment authors. Python 3.14 corpus verification is running; 208 regular tests passed on each interpreter. Public dataset/study-detail adapters remain next; this is not an A2 or cutover completion.
- Confirmed Python 3.14 real-corpus gate: all three caffeine publications plus Frost2014 persisted golden passed (4 tests); 208 regular tests on each Python, Ruff/ty clean.
- A2 study/subset checkpoint: batched complete study detail and public subset arrays match captured Frost2014 golden, including inherited subjects, ordered points, references and section notes. Study file lists enforce the same access/licence policy as downloads. 211 regular tests pass on each Python; four real-corpus gates pass on 3.14, Frost2014 full golden on 3.13; Ruff/ty pass (one corpus formatting correction applied).
- Ruling: Add study-scoped measurement source identity with composite foreign keys — legacy public subset points expose shared external-record IDs; raw and normalized measurements share this identity — cost: one typed table and nullable measurement FK, no fabricated public identifiers.
- Ruling: Scatter pairing rejects missing shared-field values — incomplete associations cannot be reliably paired; avoids silently dropping scientific points — cost: malformed sources require correction/disposition rather than partial publication.
- Golden harness correction: direct insert_graph fixture now supplies root.date like production ingestion already does; observed missing-date failure was a fixture omission, not relaxed comparison.
- A2 vocabulary/search checkpoint: public info_nodes detail/list with offline annotations, xrefs, synonyms, units, choices and parents. Exact legacy serializer goldens for apixaban/sex/male pass. Name-versus-SID HTTP filters are distinct; study creator/reference/substance filters and entity-specific related text search added.
- Inherited subject filters use a recursive lineage and nearest-ancestor window, retaining additive disease/abstinence semantics. Observed RED→GREEN for overridden choices, exclusion and predicates bound to one effective characteristic; HTTP aliases covered.
- PostgreSQL simple-dictionary full-text queries replace provisional substring matching; observed male-test/female-test false match is fixed. Four transactional GIN expression indexes added. Alembic upgrade/downgrade/check and index usability pass; vocabulary page SQL stays at five statements for 1 and 100 results. Full performance workload remains pending.
- Ruling: Explicit binary text-concatenation grouping is shared by search/index metadata — SQLAlchemy flattens ordinary concatenation while PostgreSQL reflection retains grouping, causing false Alembic drift — cost: small expression helper, no search semantics change; migration checks remain enabled.
- Ruling: Public vocabulary substance payload omits derived — captured legacy elastic serializer omits it even though model property exists — preserves actual wire contract.
- Checkpoint verification: 218 regular tests pass on each Python 3.13/3.14; Ruff format/check and ty clean. A2 remains open for full query workload membership/performance, remaining aliases/facets and compatibility matrix accounting. No retirement/cutover.
- I5 validation hardening checkpoint: 13 observed malformed-upload failures fixed. Nested container/type checks precede mapping and iteration; incorrect scalar output containers no longer publish an empty set, and description strings no longer split into characters. Recursion/nonfinite JSON failures are structured; single and truncated Pydantic reports carry accurate total error counts. Every malformed replacement regression verifies the old canonical publication is unchanged.
- Ruling: Limit source JSON nesting to 64 levels — scientific source structures are shallow; bounds interpretation/recursion work without a new service — cost: pathological deeply nested payloads are rejected. Existing nullable optional lists remain accepted where previously supported; malformed non-list containers are rejected.
- Verification: 234 regular tests pass on each Python 3.13/3.14, Ruff/ty clean; three real caffeine publications and complete Frost2014 golden pass on 3.13 after the stricter parsing checks. No source files changed.
- A5 started after validation hardening. Ruling: Implement independent explicit MCP integration now while remaining A1/A2/A3/A4 compatibility gates remain open — it consumes the already verified ingestion/query/auth/file services, not legacy draft interfaces — cost: integration may require small adjustments as remaining adapters mature; no cutover acceptance is implied.
- A5 complete: standalone FastMCP 3.4.7 mounted at /mcp/ with coordinated ASGI lifespan and MCP SDK explicitly pinned to 1.30.0. Exactly four curated tools delegate to shared services. Database bearer-token verification and execution-time rechecks preserve active accounts, roles, revocation and expiry.
- Added POST /api/v2/files and typed UUID-handle bundles. Owner/expiry/count/total-size checks precede bounded temporary materialization; opened descriptors are protected against cleanup races, then closed on every exit. Observed same-size corruption RED→GREEN after SHA-256 verification.
- A5 verification: 11 real localhost Streamable HTTP tests cover initialize/list/call, REST report/canonical parity, atomic failed replacement, anonymous/non-Bearer denial, existing-session revocation, cross-owner/path rejection, expiry/limits, concurrent principal isolation, responsive health during blocked scientific work, cancellation capacity recovery and clean server shutdown. Full suite 245 passed on each Python 3.13/3.14; Ruff/ty clean. Tests consolidated in test_transport.py because the tool contract is exercised through actual HTTP rather than separate in-process substitutes.
- Ruling: MCP handle materialization copies immutable bytes into bounded temporary source bundles and reuses the existing ingestion pipeline — avoids a second scientific/publication path — cost: an extra bounded file copy for MCP uploads; optimize only after matched profiling.
- A5 commit also fixes a test-only optional-source type assertion added after the previous ty run; current whole-tree ty check passes.
- A3 checkpoint: typed persisted subset-point identity, explicit flat analysis response schemas, seven analysis endpoints, correct pagination after output/intervention and inherited-characteristic expansion. Full Frost2014 analysis golden matches all rows and scientific values after stable identity mapping.
- Saved criteria expire after 24h, retain owner checks, and refresh active-user permissions. Bounded temporary CSV generation closes the repeatable-read database snapshot before streaming; byte failures and early close release capacity. Anonymous saved criteria re-evaluate current study visibility.
- Multi-entity concise and whole-study SQL selections now back /filter/ counts and UUID-scoped public/analysis reads. Fixed observed SQL compile failure from combining group and individual inherited-characteristic CTEs by using generated unique names. Initial combined-selection and HTTP ownership tests pass.
- Ruling: Implement advertised subsets__ criteria rather than preserve the legacy endpoint's silent omission — source handler documented subsets but never passed them onward — cost: callers previously relying on an ignored subset filter receive the requested narrower results.
- Ruling: Saved selections persist criteria rather than legacy identifier snapshots — replacement IDs change and permissions must be rechecked — cost: later reads reflect current published matching studies rather than freezing old membership.
- Verification: 254 regular tests pass on each actual Python 3.13 and 3.14; complete Frost2014 golden passes on 3.13; Ruff/ty clean after narrowing selection model typing. A3 remains open: ZIP/scatter exports, archive limits/cancellation, full saved-filter compatibility matrix and concurrent snapshot tests. No cutover.
- A3 ZIP checkpoint: /filter/?download=true returns the ten legacy members with matching MIME/filename, normalized selection, ordered CSV schemas, complete precision, bounded aggregate uncompressed bytes/rows and final archive bytes. Assets are bundled inside the package. Export construction uses one repeatable-read snapshot and closes its database session before serving the file.
- Observed disconnect-before-headers resource leak fixed RED→GREEN by closing the artifact iterator around the whole ASGI response lifecycle, including failures before the body iterator starts. Capacity/byte failures are structured before headers; early close and failures release slots.
- Captured actual legacy scatter serialization in a rolled-back transaction using four Frost measurements; new scatter output matches exact column order and scientific values and retains shared x/y point identities. Source-study files and published legacy records are unchanged. Fixture selection uses a time range because normalized floating values need not equal decimal SQL literals exactly.
- Saved-filter tests now cover removed collaborators, disabled accounts, expiry, anonymous visibility changes, ownership, early close, and replacement committing between export tables. The current archive retains its original snapshot, and the next archive evaluates criteria against the replacement.
- Verification: 257 full-suite tests passed on each Python; the two subsequently added snapshot/collaborator tests and all six saved-query tests passed on each interpreter. Full Frost public/analysis/scatter golden passed on both; Ruff/ty clean. Contract inventory now points analysis/filter routes to exports and explicitly records outstanding detail/format aliases, complete operator matrix and maintenance cleanup. A3 is not yet marked complete.
- A4 CLI checkpoint: installed argparse entry point exposes upload/validate/bootstrap/cleanup. Remote commands use one complete multipart request per study, environment-only API token, no DELETE/automatic retry/redirect, all-study JSON outcomes and nonzero failures. Sources remain byte-identical. Malformed/HTML/wrong-SID responses and timeouts cannot report success. Local bootstrap delegates to the transactional reviewed offline loader; cleanup removes expired saved criteria and lease/reference-safe file leftovers.
- Verification: ten CLI/roundtrip/cleanup tests pass, installed pkdb --help works after locked offline sync on each interpreter, full suite 269 passed on each Python 3.13/3.14; Ruff/ty clean. httpx moved from dev-only to runtime dependency using the existing lock version. A4 legacy draft protocol remains open.
- Legacy completeness investigation: 130 corpus studies omit outputset; do not require that key for draft finalization. Existing uploader always sends dataset (empty object if absent) last, after groups/interventions and optional individuals/outputs. Draft completeness must use that final protocol boundary and serialize generations, rather than reject valid studies without outputs.
- A4 draft checkpoint: owner/SID-unique SQL generations, staged reference snapshots, integer file aliases, legacy POST core/files/reference, PATCH sets, explicit DELETE and synchronous update_index finalization. Scientific publication remains the shared atomic service. Expired, foreign, malformed and incomplete drafts cannot replace publication; unknown index actions reject.
- Ruling: Treat the legacy uploader's final dataset patch as the completeness boundary, requiring groupset/interventionset to have been sent but permitting omitted optional individual/output sets — 130 current sources omit outputset; the uploader always sends dataset last — cost: clients without generation IDs must keep ordered serial upload sessions; delayed sequential cross-session payloads remain intrinsically indistinguishable and are documented.
- Observed overlapping begin/finalize stall fixed RED→GREEN with transaction-scoped try advisory locks and 409 conflicts. A subsequent new incomplete generation rejects an old finalization retry. Failed scientific finalization retains both the old publication and the draft for correction.
- Ruling: Legacy finalization uses IngestionService.replace_staged to publish verified immutable handles directly — copying/restaging would orphan the file URL returned at upload — cost: shared publisher now has a staged-handle entry point; it runs the same materialization, validation, owner/expiry/integrity checks and atomic _publish. MCP still uses its previously verified path.
- Reference ownership conflict previously surfaced as unhandled ValueError; observed HTTP regression now returns 409 with both graphs unchanged. Legacy reference response uses textual SID/PMID and omitted null fields so the unchanged uploader parser can consume it.
- Verification: 275 full tests passed on each interpreter before two final compatibility/foreign-file tests; all eight draft HTTP/service tests pass on each Python after those additions. Migration up/down/metadata checks pass; Ruff/ty clean. A4 remains open for full unchanged uploader/corpus replay, owner-only draft/reference GET/PATCH/PUT surface, additional expiry/retry cases and contract accounting. No cutover or retirement.
- I5/A1 hardening: observed oversized study/reference IDs, record keys and post-copy normalized keys now fail as source-located 422 validation errors before PostgreSQL writes; existing publication stays intact. Bounds match actual SID(255)/key(512) storage, without unnecessarily bounding free-text names.
- Dataset axis labels must be canonical zero-based ordered indices; observed accepted x/repeated/out-of-range axes now fail before persistence rather than later breaking flat analysis reads.
- Ordinary media had the same disconnect-before-headers file descriptor leak as exports. Observed RED→GREEN with a shared ClosingStreamingResponse that closes resources around the full ASGI lifecycle, including body-not-started failures.
- Verification: 285 regular tests pass on both interpreters; four actual caffeine/Frost publication/public/analysis/scatter gates pass on 3.13; Ruff/ty clean. Full read-only corpus scientific audit started in /tmp/pkdb-audit-corpus.py, output /tmp/pkdb-corpus-validation.jsonl, two local worker processes, no source/DB writes. Initial audit found unhandled PK volume-dose error in acetaminophen/Hall1976; investigate as a compatibility defect or explicit legacy-invalid source, not silently suppress.
- Corpus checkpoint: full original audit 992 valid/580 invalid/7 unexpected errors; after fixes 1,047 valid/532 invalid/zero unexpected errors across all 1,579 folders. Read-only outputs /tmp/pkdb-corpus-validation{,-v3}.jsonl; no source edits/exclusions. Remaining errors are unaccepted and require classification.
- Restored exact legacy external aliases and persisted optional group/individual/intervention image fields (Alembic 276e94c58ad1). Source digest retains original input rather than rewritten aliases. Alias collisions reject instead of silently overriding.
- Ruling: Restricted-dose eligibility follows the legacy vocabulary dimensionality check; unsupported volume/rate/area doses and mean-only doses use NaN scalar dosing while preserving dose-independent PK outputs — legacy _timecourse_to_pkdict behavior, tested analytically — cost: no dose-dependent clearance/volume without an eligible reported scalar dose. All-zero curves now return source-located pk_zero_curve errors; no artificial PK values are fabricated. Processing version bumped to 3.
- Legacy Hall1976 replay fails later in original string-key unit dimensionality normalization for auc_end, not in restored dose eligibility. Do not count full-study parity. Existing structural dimension handling remains as previously ledgered.
- Verification: 290 regular tests passed on each actual Python; four real-study golden/publication gates passed on 3.13; migration up/down/check and Ruff/ty passed. Unchanged original uploader against real local FastAPI/PG publishes copied Frost2014 (782 measurements, 70 individuals, four normalized curves), source hashes unchanged. Test initially assumed four canonical curves; corrected to eight reported+normalized and explicitly four normalized. No implementation change was needed for the uploader replay.
- Numeric subject identity compatibility: observed two failing importer regressions for integer/decimal names and numeric individual references now pass; booleans remain rejected. Source import converts only name/group/individual/parent numeric identities to text, matching legacy character-field behavior; canonical models stay strict. Processing version 4.
- Verification: 293 regular tests pass on each Python 3.13/3.14; Ruff/ty clean. Targeted read-only recheck of all 55 formerly name-type-rejected corpus studies: 43 now validate, 12 expose further structured errors, zero crashes. This is a targeted recheck, not a new whole-corpus acceptance run.
- A2/A3: observed analysis measurement_type_sid query incorrectly mapped to display-name column. Explicit measurement/substance SID aliases for group/individual analysis now retain their SID columns; focused regression RED→GREEN.
- A4: owner-only live draft/reference GET and serialized reference PATCH added; foreign and expired records return 404, anonymous requests 401, SID changes 422. Reference edits stay unpublished. A study keeps the reference snapshot selected at begin; later staged-reference edits apply to subsequent generations, documented in README.
- CLI integer study SID incompatibility observed RED→GREEN; upload now uses the same legacy integer-to-text identity convention as parser/manifest.
- A6 operational checkpoint: typed permission-checked publication metadata exposes source, processing and vocabulary digests plus current server versions. Rebuild report inventories every folder, blocks duplicate identities, checkpoints pending work before HTTP, and verifies live publication/current versions before resume. Observed concurrent replacement false-success regression fixed by comparing metadata digest with the PUT response digest.
- Ruling: Full rebuild accounting uses relative source folder paths rather than SID alone — malformed sources and duplicate SIDs cannot be represented uniquely by the plan's expected_sids sketch — cost: report consumers must inspect path-keyed results; duplicate groups remain blocked rather than silently selecting a winner.
- Verification: 297 regular tests pass on each actual Python; nine rebuild/manifest tests pass on both. Real unchanged uploader plus direct rebuild and resume gate passes on both, original Frost source hashes unchanged. Ruff/ty clean. Accidental broad tooling invocation under new Python had one expected Django-import failure in legacy-only contract characterization; no Django added to new runtime. The new independent tooling tests pass.
- Full actual corpus rebuild started against new isolated disk-backed PostgreSQL on localhost15436 and temporary FastAPI18082, script /tmp/pkdb-run-full-rebuild.py, log /tmp/pkdb-full-rebuild.log. Report and attachments in this plan's rebuild-runtime directory; disabled identities extracted from explicit source creator/curator/collaborator metadata, one test-only administrator. No production/source writes. Completion and source dispositions remain pending.

- Full corpus rebuild finished: 1,072 published, 446 failed (400 API422, 46 local bundle failures), 61 manifest-blocked (41 duplicate-SID folders, 20 malformed identities), zero pending/HTTP500. Report remains complete:false; no exclusions accepted. Durable summary and source-report fingerprint in docs/backend-migration/rebuild-checkpoint.json.
- Browser regressions format=json and search_multi_match observed RED→GREEN; explicit CORS includes upload-limit errors and /api/v1/swagger/ redirects to current docs. Existing frontend login/browse/search/Frost detail/images/output rows and selected-study archive HTTP200 verified.
- Ruling: Test legacy frontend in a temporary copy with node-sass replaced by sass under Node22 — obsolete native node-sass cannot build in this environment — cost: this does not certify the original frontend dependency build. Application source and repository frontend remain unchanged.
- Ruling: Add local create-admin bootstrap using hidden prompt or explicit stdin, rejecting existing identities — a fresh database needs its first administrator without an exposed registration privilege path — cost: operators must execute the command in the trusted backend environment.
- Runtime checkpoint: pinned multi-stage non-root images for both interpreters; REST/MCP/scientific calculation/protected files/graceful shutdown gates and actual small-fixture PostgreSQL+files restore pass on both. CI includes these gates. Full-corpus restore and replacement of legacy required-check aggregation remain pending.
- Verification: 305 regular tests and two explicit system tests pass on each Python3.13/3.14; Ruff/format/ty clean. Latest image builds include create-admin and frontend fixes. No cutover or legacy retirement.

- Full restore rehearsal passed using exported repeatable-read snapshot: 33 tables, 1,470,887 rows, 11,916 files (2,618,745,722 bytes), every ordered table row fingerprint and file SHA256/size matched; restored app ready. Evidence docs/backend-migration/restore-checkpoint.json. Isolated restore database and backup retained in rebuild-runtime/restore_99e06da0a1654f578ad3bc293c436834. Source database untouched.
- CLI output redaction observed three RED cases: token false corrupts JSON boolean; quoted/backslash token survives escaped serialization. Redact string values recursively before serialization, preserving keys/scalar types. All 12 upload CLI tests pass on both interpreters.

- Legacy .json and .json/ aliases now reuse original API endpoints/dependencies and preserve pagination URLs; private list/detail regressions observed RED→GREEN. Aliases register before dynamic SID paths to avoid consuming .json as part of an identifier. Both full suites pass: 309 tests each; Ruff/format/ty clean.
- Actual legacy GET /api/v1/pkdata/outputs/?format=csv returns404; source renderer configuration supports JSON and browsable HTML, not direct CSV. ZIP CSV members remain the compatibility surface for CSV exports. Analysis detail aliases remain open.

- Numeric output labels and characteristic choices now mirror legacy CharField int/float-to-text conversion, including zero; canonical strict fields and boolean rejection remain. Five regressions observed RED→GREEN. Processing version5. Levy1983 and McCrea1999 pass read-only validation and actual HTTP publication201 in disposable DB; Matthaei2016 now reaches existing unknown CYP1A2 genotype vocabulary errors. No source changes.
- Verification: 314 regular tests pass on each Python; Ruff/format/ty clean. Rebuilt latest images both. Broader explicit runtime/restore/Frost/scatter invocation:24 passed,5 failed on each interpreter, with identical previously recorded corpus blockers (Frost2013a missingTab3 image twice, Frost2014a unknown measurement names, Frost2015 negative concentration mean, Frost2018 vocabulary/method/dimension/negative-value errors). No claim that complete corpus suite passes.
- Existing frontend renders four Frost2014 timecourse plots and successfully downloads protected TSV via original UI. Historic complete rebuild/restore artifacts are processing4; two added studies are processing5. Rebuild/resume must refresh all publications after compatibility decisions settle.

- A1 administrative user checkpoint: POST /_users/ creates active verified accounts with Argon2 hashes and one-time returned API token; GET/PATCH/PUT detail preserves legacy field projections and read-only username. Sorted row locks recheck administrator status inside the transaction; duplicate identities roll back; role changes affect existing tokens immediately. Four original API regressions observed RED→GREEN plus stale-principal/token-role regressions pass.
- Ruling: Represent the legacy basic group as the new user role and require one fixed role (basic/curator/reviewer/admin) — the approved schema uses one application role and legacy read permission logic only understands fixed groups — cost: callers submitting multiple/custom groups must select one supported role. Arbitrary Django permission-group CRUD and validation-error shape parity remain open rather than being claimed compatible.
- Verification: 320 regular tests pass on each Python3.13/3.14; Ruff/format/ty clean. A1 is not complete; this closes the previously missing administrative user routes.

- F1/A2 performance checkpoint: new benchmark CLI waits for all expected indexed rows, joins the unchanged legacy uploader's indexing child, hashes sources before/after, records five samples, memory observations, and concrete 1.10 median/p95 budgets. Invalid queries now fail immediately; each request obeys remaining visibility deadline. Six tooling regressions pass on both interpreters; CI includes them.
- Matched local Frost2014 replacement workload: legacy median19.188s; Python3.13 median1.753s; Python3.14 median1.483s. Every measured upload/read median and p95 passes legacy×1.10. Exact source/workload equality asserted. All timed runs replace an already-published study; replacement preloads are excluded. Legacy Django development server vs single-process Uvicorn; no production or controlled cold-cache claim. Artifacts docs/backend-migration/performance/.
- Ruling: Require an explicit benchmark workload with expected counts and a SID probe for every source study — mere endpoint availability cannot prove complete asynchronous legacy index visibility — cost: operators must supply reviewed workload expectations. Initial workload used normed on timecourses, a parameter not declared by legacy (silently ignored); corrected it rather than add unsupported filter semantics. Original incomplete replacement measurement remains scratch evidence; reran matched supported workload.
- Full-corpus read-only execution plans captured for1,074 studies/361,534 measurements: study search0.327s, filtered outputs0.102s, same-measurement predicates0.064s, statistics0.141s. Plans preserve actual index/buffer evidence. New study-page bounded-query test passes on both Pythons; initial fixture wrongly reused the schema's unique reference ownership, corrected distinct matching source/reference SIDs without application changes.

- Legacy account validation: captured actual Django missing/blank/email-invalid responses and observed six mismatches RED→GREEN. Account/admin model-field errors now HTTP400 field mappings without echoing credentials. Administrator dependency runs before field validation; transaction authorization still rechecks active current role. Unrelated public path validation remains unchanged. Complete malformed-body/private-email ordering matrix remains open.
- Analysis detail GET implemented for study SID, intervention PK, timecourse subset PK, including .json aliases and saved-filter visibility. Initial timecourse fixture lacked nine PK vocabulary definitions; after fixing fixture, disabled route produced404 RED, restored route matched all list records GREEN. Association detail identifiers remain unresolved; no guessed identity mapping.
- Verification:330 regular tests pass on both interpreters; Ruff/format/ty clean. Processing5 read-only audit complete:1,093 valid/486 invalid/zero unexpected across1,579 folders. Durable summary/hash and sampled failure code/location TSV committed; no source edits or accepted exclusions.

- F1 benchmark policy completed: each runtime now records one fresh-application cold run, one excluded warmup, and five measured warm runs. Seven benchmark regressions pass on both Python versions; all16 independent migration tooling tests pass. Cold upload-to-visible: legacy20.622s,3.13 2.809s,3.14 2.385s; warm medians3.13 1.775s,3.14 1.485s. All measured median/p95 budgets pass. Updated raw artifacts and required docs/backend-migration/performance.json aggregate.
- Ruling: Cold benchmark means fresh application process before any HTTP preparation request, with an already-published study; host/database/search caches remain intact — flushing shared host caches would disturb other workloads and is not needed to compare application initialization — cost: completely cold storage/cache latency remains unmeasured. Artifacts explicitly state the limit.
- Account authentication ordering: six regression failures confirmed malformed private bodies bypassed permissions and email field/path errors preceded authentication. Email router dependencies now authenticate before field validation; malformed JSON handler executes protected-route authorization in worker thread and delegates denial to existing handlers. All15 focused tests pass. Initial test append used a duplicated backend-next path and did not write; corrected before RED. Tooling pytest executable lacked repository import root; python -m pytest passes both, no runtime change.
- Account-order verification:337 regular tests passed on each Python3.13/3.14; Ruff/format/ty clean. Six RED cases all GREEN, including anonymous malformed JSON on .json admin alias and nonadmin denial before malformed-body errors.

- User requested save and stop for another machine. Current account checkpoint4499eb8a:337 regular tests and two actual image/restore system tests pass on each interpreter; images50549f3143f0(3.13),cc2413f9b6b3(3.14).
- Role catalogue checkpoint added after that full verification: six focused tests pass on3.13 (five missing-route RED cases plus pagination underflow RED→GREEN); complete suites and latest images have NOT been rerun after this final addition. GET list/detail preserves captured four-role projection, JSON suffixes, pagination and admin-only reads.
- Ruling: Role catalogue uses fixed fresh IDs1–4 for basic/admin/reviewer/curator and empty permission arrays — matches captured role names/projection and avoids Django content-type permission tables — cost: historical/custom group identifiers and mutations are not preserved; compatibility disposition remains required.
- Paused at user request; no final review, cutover, merge, push, deployment or legacy retirement. See committed handoff.md for machine transfer.

- User subsequently requested commit everything and push. Saved original-workspace additions as e82c21f3 on handoff/local-workspace-20260922; merged into migration branch8816c143. Develop ref unchanged. Legacy uploader inclusion is now explicitly requested. Template credential replaced with placeholder; original only in protected local backup. Generated handoff artifacts excluded. Auto-review rejected the push because newly added legacy resources include personal account data and target matthiaskoenig/pkdb is public; explicit payload/destination approval pending. No push succeeded.


## Native execution resumed on the transferred machine (2026-09-22)

- Restored bundle commit a49e751d into an isolated clone at /tmp/pkdb-backend-replacement on backend/fastapi-replacement. The original checkout remains on handoff/local-workspace-20260922 and its transfer directory is unchanged. Read the handoff, execution rulings, design, plan index, current acceptance and runbook before resuming.
- Recreated separate locked environments with CPython 3.13.15 and 3.14.6. Started disposable PostgreSQL 18.0 container pkdb-migration-resume-postgres on 127.0.0.1:15435; existing application containers were not changed. Network and Docker sandbox restrictions required escalated test execution. Initial ty invocation selected system Python 3.12; explicit --python .venv-py313/.venv-py314 corrects environment selection and both checks pass.
- Completed the deferred role-catalogue baseline: 343 regular tests and two runtime/restore system tests pass on each interpreter; 16 migration tooling tests pass on each. Both pinned images build. Initial restore attempts failed at pg_dump while ordinary suites were creating/dropping schemas in the same database; a diagnostic rerun and subsequent sequential gates pass. The initial subprocess stderr was not retained, so the precise failure cause is not established. Keep restore gates separate from concurrent schema churn on this machine.
- A1/A4 HTTP matrix continuation: reproduced anonymous malformed POST /api/v1/_references/ returning 422 instead of 401. Added ten HTTP regressions covering malformed JSON and non-object JSON across reference POST/PATCH, study POST/PATCH and update_index. Router authentication now precedes body/path validation; the exception adapter authenticates malformed FastAPI model bodies, whose decoding otherwise precedes dependencies. Existing publication and draft authorization checks remain in their services.
- Final verification after that fix: 353 regular tests pass on each CPython 3.13.15/3.14.6; Ruff/format and both ty checks pass. Both sdist/wheel builds and clean-wheel scientific imports pass. Alembic upgrade/check/downgrade/upgrade passes on both interpreters with no metadata drift. Latest images baf4f1c41535 (3.13) and 36e01290a676 (3.14) each pass the two runtime/restore tests. Two existing Starlette dependency deprecation warnings remain.
- Corpus disposition question was offered again; no answer received at this checkpoint. Continue strict validation, no source changes and no approved exclusions. The corpus is available at /home/USERNAME/git/pkdb_data/studies, not the isolated clone's sibling directory. No current full-corpus rebuild or fresh full-corpus acceptance is claimed.
- Next: published editable study GET/PUT/source roundtrip. Legacy StudySerializer returns file URLs and curator projections, so its GET result is not directly replayable as the current staged input. Characterize that distinction before implementing; canonical storage still lacks original study/reference JSON templates. Administrative reference/file/vocabulary operations, association detail identities, remaining filter/error/media matrices and final independent review remain open. No cutover, publication, merge or deployment occurred.

## Develop integration authorized (2026-09-22)

- The user explicitly requested merging all backend changes into develop. This authorizes integration of the additive backend-next work, not legacy retirement, production deployment or a claim of completed corpus acceptance. The earlier no-merge pause is superseded for this integration.
- Fresh independent whole-branch review found no additional merge-blocking regressions; legacy runtime/deployment remains intact. The reviewer also inspected the subsequent client-test isolation fix. Reviews were static; execution evidence follows.
- Pre-merge CI checks exposed six import-order failures and newly preserved client tests importing modern metadata APIs into the Django Python 3.9 suite. Moved those tests intact into backend/client-tests, with a separate locked Python 3.13 project and test-only dependencies. Legacy backend dependencies remain unchanged. CI's required tests aggregate now also requires this client suite and its type check.
- Replaced the client reference test's live PubMed request with synthetic XML at the transport boundary. The old workbook test referenced a missing fixture without asserting success; it now creates a temporary workbook and checks actual TSV content. Vocabulary tests use isolated cached responses and a public registry subset; external network calls are disabled. No tests are skipped.
- Verification: 108 legacy backend tests pass against the dedicated PostgreSQL/Elasticsearch test services; 33 isolated client tests and their type check pass. Root lint/format and legacy type checking pass. Previously recorded replacement verification remains 353 tests per Python and both runtime/restore image gates; replacement source has not changed in this integration fix.
- Remote inspection confirms the legacy source checkpoint e82c21f3 already exists on origin/handoff/local-workspace-20260922. Integration is proceeding through the repository's required pull request and checks, not a direct develop push or a ruleset bypass.


## 2026-09-22: retire the previous backend source

- Explicit user instruction supersedes the earlier source-retirement hold: remove the previous backend and simplify local setup. This is repository cleanup, not production cutover or approval of unresolved scientific/compatibility gates.
- The current implementation moves from `backend-next/` to `backend/`. Django, Elasticsearch deployment, the old uploader/client tests, old environment templates, destructive deployment helpers, nginx configuration, and legacy-only export/benchmark tooling are removed. Historical evidence and provenance remain.
- Root Compose starts only FastAPI and PostgreSQL, with distinct persistent volumes, automatic Alembic migrations and vocabulary bootstrap. It binds localhost:18083.
- `pkdb bootstrap-study` creates disabled attribution identities from read-only study folders without changing existing accounts. Zensical documents the complete Docker validation/upload workflow.
- Python 3.13 and 3.14: 382 regular/tooling tests pass on each. Ruff and ty pass. Zensical builds successfully. Real Frost2014 Docker validation, first upload (201), replacement (200), authentication after restart, persisted study equality and source-file hash preservation pass. Image lifecycle and database/attachment restoration are verified separately.
- Existing operational services, source data and volumes are preserved. Historical corpus failures and acceptance gaps are not reclassified as passing.


## 2026-09-22: restore vocabulary authoring

The `pkdb_data` definitions are authoritative scientific source data and should not have been removed with the previous backend. Restore the focused authoring package, its ontology/chemical metadata caches, and the full cached identifiers.org registry. Django, the uploader, and deployment utilities remain retired.

`scripts/update_vocabulary.py` regenerates vocabulary and provenance JSON offline. The complete 2,147-node content is preserved byte-for-byte; only the snapshot version changes to a hash of generated node content. Scientific policy constants are restored from the original source without changing their semantics. Source validation reuses the current bootstrap validator. CI checks generated output against editable sources.

A source-to-database regression test adds a categorical measurement, its allowed choice, and a substance with explicit chemical properties and no cached ChEBI entry. It also verifies that an invalid parent leaves generated JSON and users unchanged.
