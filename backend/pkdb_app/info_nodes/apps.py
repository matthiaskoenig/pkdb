"""Django app configuration for the info_nodes app."""

from django.apps import AppConfig


class InfoNodesConfig(AppConfig):
    """Register the info_nodes app under the name ``info_nodes``."""

    name = "info_nodes"
