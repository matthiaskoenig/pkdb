"""Django app configuration for the comments app."""

from django.apps import AppConfig


class CommentsConfig(AppConfig):
    """Register the comments app under the name ``comments``."""

    name = "comments"
