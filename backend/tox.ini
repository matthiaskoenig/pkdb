[tox]
envlist = ty, py3.9

[testenv]
# editable: manage.py and the settings module are used from the checkout
package = editable
extras =
    dev
passenv =
    PKDB_*
commands =
    pytest {posargs}

[testenv:ty]
passenv =
    TY_OUTPUT_FORMAT
extras =
    dev
commands =
    ty check
