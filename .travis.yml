sudo: required
language: python
services:
  - docker

python:
  - "3.6"

before_script:
  - docker-compose build

script:
  - docker-compose run --rm backend bash -c "./manage.py test"

notifications:
  email: false

cache:
  pip: true

